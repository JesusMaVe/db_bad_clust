database:
  user: "bad_schema"          # Usuario APP_USER que configuraste
  password: "bad_schema_pass" # Contraseña del APP_USER
  dsn: "localhost:1521/FREEPDB1"  # Service name por defecto

project:
  name: "ml_bad_db_trainer"
  version: "1.0.0"
